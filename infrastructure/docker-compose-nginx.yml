version: '3'

services:
    balancer:
        image: 'nginx:latest'
        ports:
          - 80:80
        volumes:
          - ${PWD}/nginx.conf:/etc/nginx/nginx.conf
    web-one:
        image: 'php:7.3-apache'
        volumes:
            - ./website/:/var/www/html/
    web-two:
        image: 'php:7.3-apache'
        volumes:
            - ./website/:/var/www/html/
